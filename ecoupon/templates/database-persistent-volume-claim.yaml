apiVersion: v1
kind: PersistentVolumeClaim
metadata:
    name: database-persistent-volume-claim
spec:
    accessModes:
        - ReadWriteMany
    storageClassName: managed-premium
    resources:
        requests:
            storage: 1Gi