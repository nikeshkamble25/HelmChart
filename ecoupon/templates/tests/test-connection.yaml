apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "ecoupon.fullname" . }}-test-connection"
  labels:
    {{- include "ecoupon.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "ecoupon.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
